package main

import "fmt"

func main() {
    // Cara 1: Deklarasi lengkap
    var x int = 10
    
    // Cara 2: Short declaration (CIRI KHAS GO)
    y := 20
    
    // Tipe data otomatis
    nama := "Iqbal"
    aktif := true
    
    fmt.Println(x, y, nama, aktif)
}
